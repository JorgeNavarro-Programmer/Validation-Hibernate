server:
  port: 8080

spring:
  application:
    name: validation
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:clientsvalidation;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: mysql
    database-platform: org.hibernate.dialect.H2Dialect



